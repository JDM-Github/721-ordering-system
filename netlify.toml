[build]
  node_bundler = "esbuild"
  functions = "functions"
  publish = "client/build"
  command = "npm install && npm run build"

[dev]
  functions = "functions"
  publish = "client/build"
  node_bundler = "esbuild"


[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
